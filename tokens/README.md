# Tokens

Design tokens — the single source of truth for visual primitives.

**What lives here:** color, typography, spacing, radius, elevation/shadow, and motion tokens, ideally in a portable format (JSON / W3C design-tokens) that can feed both Figma and the Tailwind config.

**Consumed by:** `../design-system/` and the app's Tailwind theme.
