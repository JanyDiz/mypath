# Design System

The assembled, documented system: tokens + components + usage rules.

**What lives here:** the cohesive library and its documentation — foundations (from `../tokens/`), the component catalog (from `../components/`), patterns, accessibility guidance, and do/don't usage rules. This is what designers and engineers reference day-to-day.
