# Wireframes

Low- to mid-fidelity layouts and flows for mypath, before visual design.

**What lives here:** screen skeletons, user flows, and structure for the core journeys — JD intake → format select → question → answer (text/voice) → rubric feedback → progress.

**Naming:** `NN-flow-screen.ext` (e.g. `01-jd-intake.png`, `02-format-select.png`).
