# Components

Individual UI component designs and specs.

**What lives here:** per-component artwork and specs (states, variants, anatomy, behavior) — e.g. buttons, inputs, the JD paste box, the recorder, the rubric score card, the feedback panel.

Built from `../tokens/` and assembled into `../design-system/`.
